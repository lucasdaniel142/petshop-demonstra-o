{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome (Localhost)",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}